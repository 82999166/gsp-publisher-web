# 三个核心功能实现计划

## 1. 完善 embed 插入逻辑

### 当前问题
- embed 块虽然被插入了，但在发布后看不到
- 可能原因：Google Sites 的 CSP 限制或 iframe 渲染问题

### 解决方案
1. **支持多种 embed 类型**：
   - iframe（通用）
   - Google Maps
   - YouTube 视频
   - Google 表单
   - Google 日历
   
2. **改进插入流程**：
   - 检测 URL 类型，选择合适的插入方式
   - 对于不支持 iframe 的内容，使用 Google Sites 原生支持的方式
   - 增加错误处理和重试机制

3. **验证插入成功**：
   - 插入后截图验证
   - 发布后再次验证内容是否显示

### 实现步骤
- [ ] 修改 `insertEmbedBlock` 函数，支持多种 embed 类型
- [ ] 为不同类型的 URL 添加特殊处理逻辑
- [ ] 增加插入验证和错误处理
- [ ] 测试各种 URL 类型

---

## 2. 样式自动应用

### 当前问题
- H1、H2 等标题的样式设置（字体大小、粗细、对齐等）没有被应用

### 解决方案
1. **样式应用流程**：
   - 写入文本后，选中文本
   - 打开样式菜单
   - 应用对应的样式设置

2. **支持的样式**：
   - 字体大小（小、正常、大、特大、超大）
   - 字体粗细（常规、中等、半粗、粗体）
   - 文字对齐（左对齐、居中、右对齐）
   - 文字颜色
   - 背景颜色

3. **实现方式**：
   - 在 `writeByTemplate` 中，为每个板块应用对应的样式
   - 使用 Google Sites 的样式按钮进行应用

### 实现步骤
- [ ] 分析 Google Sites 的样式按钮位置和选择器
- [ ] 修改 `writeByTemplate` 函数，添加样式应用逻辑
- [ ] 为 H1、H2、段落等不同块类型应用对应的样式
- [ ] 测试样式应用效果

---

## 3. 批量发布优化

### 当前问题
- 批量发布时，某个任务失败会导致整个批次中断
- 没有重试机制
- 没有发布队列管理

### 解决方案
1. **发布队列管理**：
   - 为每个发布任务添加状态（待发布、发布中、已发布、失败）
   - 支持暂停/继续/取消操作
   - 显示发布进度

2. **失败重试机制**：
   - 发布失败时自动重试（最多 3 次）
   - 重试间隔逐步增加（1 分钟、5 分钟、10 分钟）
   - 记录失败原因

3. **并发控制**：
   - 支持自定义并发数（1-10）
   - 避免过多并发导致 Google Sites 限流

4. **发布日志**：
   - 详细记录每个任务的发布过程
   - 支持查看日志和下载

### 实现步骤
- [ ] 修改数据库 schema，添加发布队列表
- [ ] 实现发布队列管理逻辑
- [ ] 添加失败重试机制
- [ ] 优化并发控制
- [ ] 更新前端 UI，显示发布队列和进度
- [ ] 测试批量发布流程

---

## 实现优先级
1. **高优先级**：完善 embed 插入、样式自动应用
2. **中优先级**：批量发布优化

## 预计工作量
- 完善 embed 插入：2-3 小时
- 样式自动应用：3-4 小时
- 批量发布优化：4-5 小时
- 总计：9-12 小时
